const express = require("express");
const router = express.Router();

//GET route for viewing all departments in inquirer
router.get('/', (req, res) => {
});
  
//POST route for adding departments from inquirer
router.post('/', (req, res) => {
});
  
module.exports = router;